# Roadmap and status

The authoritative status of each work area, with the evidence for the claim.
Recorded here because two other documents note that the roadmap "is not
recorded anywhere in this repository", which made every phase claim
unauditable.

Scope note: this is the **work-area** roadmap. The separate *Phase 0–12*
numbering referenced by `dataset-benchmark-plan.md` and
`benchmark-acquisition-plan.md` is still not committed anywhere, so the phase
numbers in those documents remain inferred, exactly as they say.

Last verified against `9efb362` on 2026-08-26 (autonomous build pass; see the two new rows below).

| Area | Status | Evidence |
|---|---|---|
| Core backend / data platform | ✅ Complete | 2,188 tests pass in Docker, re-verified 2026-08-22; 1,167 files and 749,315 traces ingest across four formats and three vendors (that ingest figure is from 2026-08-08, not re-checked this pass) |
| Open GPR dataset investigation | ✅ Complete | `dataset-inventory.md`, `dataset-benchmark-plan.md`, `cmugpr-acquisition-assessment.md` |
| BAM benchmark acquisition | ✅ Complete | `external-gpr-benchmark-acquisition.md`; acquired and checksum-verified |
| BAM detection benchmark | ✅ Complete | `artifacts/bam/*.json`, 161 lines scored at both frequencies |
| 4TU utility benchmark | ✅ Complete | `artifacts/4tu/benchmark.json`, complete **125**-activity corpus |
| Benchmark test infrastructure | ✅ Strong | GPR regression 26 pass bit-identical (re-verified: 26 tests still collect); baseline identity 12/12 |
| Ground-truth / provenance safeguards | ✅ Established | `provenance.md`; enforced in backend tests and in the UI |
| Localisation / X-Y-Z scoring | 🔒 **BLOCKED** | Both artifacts carry the gate; 4TU publishes no trench coordinates, BAM's absolute origin is unverified. Unaffected by everything below — see "What blocks localisation". **4TU localisation remains blocked; fallback benchmark search complete, none integrated** — `docs/fallback-localisation-dataset-search.md` (2026-08-22) scored a newly-identified candidate (Grimsel ISC, ETH Zurich) at 59/100. **Deep audit follow-up (2026-08-22)** — `docs/grimsel-deep-evidence-audit.md`: re-scored **73/100** on primary-source evidence this pass fetched directly (the borehole/shear-zone dataset's real Swiss-grid-tied coordinate origin, and its separate CC-BY-4.0 license, distinct from the GPR data's own non-commercial-only license). **Geological-model file audit (2026-08-22)** — `docs/grimsel-geological-model-file-audit.md`: downloaded and read the geological-model MATLAB toolkit directly (CC-BY, 2.87 MB, checksummed, deleted after reading). Found the AU tunnel's own path in absolute Swiss-grid coordinates (`AUTunnel.txt`) and the borehole-to-shear-zone computation path (collar + azimuth/dip + intersection depth, `FBS.txt`/`S3_1.txt`), sharpening the score to **78/100** — still below 80. The GPR profile's own placement in the authors' visualization (`plot_GPR.m`) is a manually-fit overlay (Category C, approximate), not a surveyed tie — the one fact still missing is which tunnel point corresponds to the GPR's own position zero. Classified **B — conditional benchmark**, unchanged. Not integrated; author questions sharpened, not sent. TU1208 and BAM remain the strongest already-held partial (X+depth, non-geodetic) candidates. **TU1208 physical-depth validation run (2026-08-22)** — `docs/tu1208-physical-depth-validation-report.md`: X 🔒 blocked (no profile along-line origin published), Y 🔒 blocked (all 36 surveyed targets carry `transverse_offset_m: None`), Z 🔒 blocked for end-to-end validation (ground truth and conversion machinery both exist and are honestly labelled, but 0 of 36 targets could be tied to a measured reflector, so zero depth estimates were produced — Outcome D, insufficient evidence, not a partial success). No dataset has been integrated and no readiness state here has changed |
| Composition honesty — the off-GPR invitation cascade | ✅ Complete for the surfaces audited | 23+ commits (keyword search on `git log`, likely an undercount): every surface that used to say a GPR-only capability "has not been run" for a non-GPR dataset (report, radargram viewer, thin-client, workspace panes, import-report, exports, candidates inspect route, dataset list/switcher/summary, device registration/card) now says it does not apply and names the actual recorded/declared composition instead. `dataset.sensor_type` is labelled "declared" everywhere it surfaces, never presented as the recorded instrument |
| Inventory D — DEM as a first-class modality | ✅ DEM closed; other members open | `SensorType.DEM` added next to `LIDAR` (`schemas/subterra_record.py`); GeoTIFF's undeclared-band elevation inference fires for LIDAR or DEM (`docs/surface-reference.md`); import picker and device registration both offer the full ten-member enum, no pre-selected default, no `other` (a value the backend never accepted). Held reference datasets (COP30, AHN) intentionally not migrated. `ert`/`gravity`/`gps`/`imu` still have no dedicated converter — declarable, not yet ingestable as their own modality |
| Fusion: visibility, redaction, and a run control | ✅ Complete | `GET`/`POST /api/fusion/*` apply the same visibility rule as the dataset list; another user's dataset id inside a shared sample is redacted (`dataset-not-visible`), not shown or dropped; `/fusion` is a real UI (preview then save — `persist` has no dedup against stored samples, so it defaults to `false` and Save is disabled until a preview matches the current configuration by value) |
| Frontend architecture audit | ✅ Complete | `frontend/README.md` |
| V0 frontend migration | ✅ Complete | workspace, datasets, benchmark and marketing landing pages all ship. **Correction (2026-08-24): the "landing page still not ported" line above was stale, not current.** `frontend/app/page.tsx` mounts the full `Descent` composition (all 9 stages, `LandingNav`, `Bridge` transitions) in commit `dc47d77` (2026-08-08) — two weeks BEFORE the 2026-08-22 "re-checked" note that claimed no route mounted it. Re-verified live (real Chromium via Playwright MCP, dev server on :3000): page loads with the correct title, zero console errors/warnings, full 9-stage content renders (Surface through View), and "Enter the workspace" genuinely navigates to `/datasets`. The false claim is corrected here rather than left standing — this file exists specifically so a status claim is auditable, and this one wasn't |
| Real API → new frontend | 🟡 Substantially complete | 10 of 12 backend route groups have UI (`exports` added, 2026-08-24: an `ExportPanel` on the candidates page, gated on `classified_object_count > 0` — real formats from `GET /formats`, real file download, honest write/skip report where the transport supports one). **Currently unreachable for any real dataset**: `classified_object_count` is hardcoded to 0 at both call sites in `api/reports.py`, and `database.objects_store.save_objects` has no live caller anywhere in the product — no classification pipeline exists yet, so the panel correctly renders nothing rather than fabricate a state. `sources`, `training` still have no UI — `sources` (external DEM/earthquake fetch) and `training` (model training, deliberately deferred: exposing it next to a chance-level baseline detector without care could mislead) are open product decisions, not oversights |
| Browser verification | ✅ Complete through the Phase 7 sweep | `browser-verification.md` Pass 2 — run 2026-08-22 against `22b08ba`, real Chromium via Playwright MCP. Covers everything Pass 1 (2026-08-08, `0daa3e7`) predated: composition honesty across five surfaces, the full DEM import workflow (enum, no default, elevation-inference checkbox, ready screen), device registration with multi-modality declaration and reload persistence, `/fusion` preview-then-save end to end (200,288 records, 9 samples, redaction confirmed by a second test account plus the passing dedicated test), and the generic 422 serializer confirmed live via a real oversized-password submission. No regressions, no genuine product bugs found; backend 2,188/2,188 and frontend 570/570 pass against a freshly built image/current tree, typecheck/lint/build all clean |
| Detection improvement | ⏳ Open — one candidate tried and **rejected** | `detector-multiscale-experiment.md` |
| Learned Detector V1 — segmentation | 🔒 **BLOCKED by insufficient real labels** | `schemas/segmentation.py`, `training/segmentation.py`, `training/segmentation_model.py` (61 tests, all passing). A real-data audit of every held GPR dataset's own truth file (not a doc's summary of one) found exactly **4 real, trace-associated targets in the entire corpus, all in one BAM specimen (Pk266)** — 4TU is a per-activity count with coordinates withheld, TU1208 is depth-only with `transverse_offset_m` populated for 0 of 36 targets, TestUM's truth is a diffuse freezing front with no discrete object, Grimsel is not held. One site cannot support a genuine site-level train/validation/test split (`assess_split_adequacy` requires ≥2 sites per split and correctly flags the real BAM case as inadequate) or a statistically meaningful training claim. Per this milestone's own governing rule, **no training run was attempted and no labels were fabricated** — only the reusable dataset/split/metric/baseline-comparison infrastructure was built and tested, ready for real labels the moment more exist. **A real, valuable finding came out of the audit anyway**: scored against the 4 real target locations (not benchmark-level scoring), the current statistical detector's z-score never exceeds ~2.0 at the true target cells — under the |z|≥3 threshold production candidate generation actually uses, giving 0 recall at every real target when scored at that threshold. This is the first time the platform's long-standing "near chance" finding has been confirmed against genuine measured ground truth rather than aggregate benchmark scores. **Exact annotation deficit for a real Detector V1 attempt**: real trace-to-target association (not just existence/depth) for more sites than the one BAM specimen already has it, ideally spanning independent sites/vendors/frequencies; verified negatives at the same precision. Recommended next milestone: a real GPR annotation corpus effort, not further model work |
| Human-in-the-Loop Anomaly Verification V1 | ✅ Complete, plus 4 follow-on phases | Temporary dev/training infrastructure, NOT the customer workflow: a knowledgeable reviewer turns real Subterra candidates into provenance-backed annotations for a future Learned Detector, per its own 29-section spec. `schemas/review.py` keeps `ReviewStatus` (unreviewed/confirmed/rejected/uncertain) structurally separate from the pre-existing `CandidateStatus` -- different questions ("genuine evidence" vs "worth retaining"). Every review is `EvidenceGrade.C_OPERATOR_REVIEWED`, never promoted to A/B; a REJECTED candidate never becomes a verified-empty ground-truth negative; history is append-only; detector output is frozen at first review. 7 endpoints under `/api/reviews`, a corpus exporter reusing `training.segmentation`'s existing QA/manifest logic verbatim. **Follow-on 1: canvas-based annotation** -- click/drag rectangle and ridge-path annotation directly on `RadargramCanvas` (additive `mode` prop, default preserves the original 45-test behaviour byte-for-byte); a real bug caught in live browser verification (a drawn region outside the candidate's own box produced negative mask indices) was fixed via `effective_trace_range`, unioning both extents. **Follow-on 2: review queue prioritization** (Section 14) -- sort the candidate list by position or highest score, plus an inline already-reviewed badge per row; live-verified against the real Path8 dataset (13 candidates), sort reorders 5.42 -> 3.37 correctly. **Follow-on 3: consensus-review schema readiness** (Section 16) -- a computed `review_kind` (`single_review`/`consensus_review`) derived from `history`'s own reviewer_ids, no new stored field or workflow; genuinely exercisable only at the schema/store layer today since ownership restricts review-writing to one dataset owner. **Follow-on 4: 3D scene review-state display** (Section 19) -- selecting a scene candidate shows its existing human review read-only ("Human review: Confirmed · pipe"); honestly limited in live verification since this platform's 7 real datasets currently have no *resolved* 3D scene with a placeable candidate, so this was verified via 24 tests (2 new) against a realistic fixture rather than a live click-through. Sections 15/17/20-23 of the spec were already satisfied by the core milestone. 73549a1, 43e089f, f2648aa, bf5e6b8, 79e2136 |
| Topographic/air-gap correction | 🟡 Implemented and live-wired; no live dataset can reach its DERIVED path yet | Follows time-zero correction's own two-stage precedent (framework first, wired into a live endpoint once the design was clear). `schemas/topographic_correction.py` + `preprocessing/topographic_correction.py` productionize the exact methodology the 2026-08-22 research audit validated (`scripts/four_tu_topographic_correction_audit.py`): `height_above_ground = antenna_elevation - dem_ground_elevation`, differenced against the line's own median so an unknown constant vertical-datum offset between the two sources cancels out, reported `derived` only when the resulting correction exceeds the line's own sample interval (`not_material` otherwise -- a complete, honest, non-actionable answer, never conflated with `unavailable`). A real correctness bug was found and fixed alongside this: `preprocessing.dem_alignment.align_records_with_dem` overwrote `record.elevation` with DEM ground elevation unconditionally, discarding an antenna's own GNSS reading with no way to recover it; it now preserves the prior value into `metadata["pre_dem_elevation_m"]` first. `POST /api/datasets/{id}/apply_topographic_correction` takes no DEM parameter of its own -- that question is already answered by the existing `/align_dem` endpoint -- and refines `corrected_time_ns` (from time-zero) into a new `topographic_corrected_time_ns`, never overwriting it. **Live-verified against the real, running product** (real `/align_dem` + `/apply_topographic_correction` calls against the real 4TU Path8 demo dataset and the real AHN site01 DEM tile, records-file checksum confirmed unchanged before/after): correctly reported `unavailable` end to end, because this specific dataset has no position data the DEM tile covers AND no antenna elevation to begin with. **A further, more consequential finding came out of that live check**: NO dataset ingested through the live product's actual `/ingest` or `/ingest_local_file` endpoints can ever populate an antenna elevation at all -- `coordinate_encoding="ieee_nmea"` (the one encoding that parses it) is used only by the research audit script directly, never exposed as a parameter on either live ingest endpoint. The DERIVED/material path of this feature is therefore unreachable by any dataset in this product today, not just the one tested. Exposing `coordinate_encoding` on the general ingest surface would fix this but is a real product-UX decision (it would appear on every future ingest, not only this feature) -- left for a deliberate choice, not made unilaterally. **A second gap was caught immediately after**: the correction's own `as_processing_applied` docstring claimed a future dataset-report reader would pick it up "the same way it already reads `time_zero_*` keys generically" -- checked, and false: there is no generic reader, so without a fix the correction would apply and persist correctly but never appear in the dataset report's own signal-processing chain, the platform's actual "what happened to this dataset" surface. Fixed with `_topographic_correction_step` (mirrors `_local_anomaly_step`: omitted entirely for the vast majority of datasets that never ran the endpoint, not reported as "not run"). e81d60b, c6dd40d, 9efb362, 1017d82 |
| Real GPR Annotation Corpus V1 | 🔒 **STILL BLOCKED, exhaustively re-confirmed** | `schemas/segmentation.py` (`EvidenceGrade`, a third axis distinct from spatial precision and association mechanism — A independently verified / B measurement-associated / C operator-reviewed / D weak-unsuitable, D never enters primary training), `training/segmentation.py` (`validate_corpus`, `build_corpus_manifest`, `annotation_record`, `render_annotation_overlay` — 23 new tests, 84 segmentation tests total). Two independent re-audits, neither changed the count: (1) a NARROWER re-ask of TestUM/Grimsel/BAM Pk401 — each has a concrete, specific reason it stays excluded (TestUM's only "truth" would be the experiment's own operation, not an independent measurement; Grimsel's real S3 shear-zone truth is genuinely strong but the GPR profile's own tie to the tunnel path is missing from everything held; Pk401's archive was never even downloaded, and wouldn't help site-independence regardless). (2) Three genuinely new datasets, never inventoried in any prior session, found and run to ground: a Grimsel AU-tunnel GPR file (non-commercial licence, no coordinate field), a Guangzhou University pipe/rebar dataset (CC-BY-4.0 — ruled out conclusively by parsing its full 5,046-member archive listing directly via HTTP range requests against the zip's own central directory, a few MB not 3.8 GB: zero target/coordinate files exist in it), a Hillside dataset whose own provenance claims "surveyed control points" that turned out to be an unverified tag — all 321 of its coordinate files are 0 bytes. (3) A genuinely new, narrower external search (not a repeat of the prior broad localisation search) found a real Morocco utilities/voids dataset with actual bounding-box annotations, rejected on three independent grounds (CC BY-NC non-commercial; ground truth is operator visual interpretation only, Evidence Grade C not A/B; only rendered JPEG images published, no raw trace data at all — structurally incompatible with this platform's real-amplitude pipeline), plus two real controlled pipe test beds (Sense-City Paris-Est, Zhejiang University) described in real papers with no discoverable public data repository for either. **Conclusion, now exhaustively documented rather than assumed: still 4 real targets, still 1 site.** The corpus infrastructure (evidence grading, QA validation, versioned manifest, visual QA overlay) is built, tested, and ready the moment real multi-site evidence exists — no training was attempted, no labels were fabricated, no evidence standard was lowered to inflate the count |
| Self-Supervised GPR Encoder V1 | 🟡 **B — PROMISING BUT VARIABLE** (refined by a robustness ablation) | `schemas/ssl_gpr.py`, `training/ssl_corpus.py`, `training/ssl_masking.py`, `training/ssl_model.py`, `training/ssl_train.py`, `training/ssl_augment.py`, `training/ssl_probe.py`, `training/ssl_ablation.py` (85 new tests, 2,636 backend tests total). NOT a detector — masked-reconstruction pretraining only, no target labels used anywhere in training. Real unlabelled-corpus audit: 1,348 real source files/lines across 5 commercial-licence datasets (BAM CC0-1.0, 4TU CC0-1.0, TU1208-IFSTTAR CC-BY-4.0, Hillside-Lancaster CC-BY-4.0, TestUM/PANGAEA CC-BY-4.0) plus one real file kept in a separate research-only pool (Grimsel AU-tunnel, InC-NC, never combined with the commercial corpus); `ingv-unisa` excluded (license unverified, and structurally not multi-sample trace data). A real site-level policy reserves `testum` entirely (never touched by SSL train or validation — a genuine unseen-site holdout) and two 4TU sites (`012`/`013`) the same way; two more 4TU sites (`010`/`011`) are SSL-validation-only; every other split is `UNSEEN_LABELS_SEEN_ACQUISITION`, never conflated with `UNSEEN_SITE`. Architecture: a 3-level, 109,289-parameter encoder extracted from the existing `TinyUNet` tower, paired with a reconstruction decoder that has **no skip connections** (Section 12's own leakage requirement) so a masked region's real value can only reach the loss through the compressed bottleneck. Masking: contiguous trace-block, ratio 0.3, real GPR structure preserved (not independent-pixel masking). Real training run: 3,200 train / 800 validation windows stratified across all 4 commercial datasets, 3 epochs, CPU, deterministic seed. **Real result**: train loss 1.19→0.94; validation loss beat both baselines clearly (trained 1.07–1.13 vs random-init-encoder 1.46 vs zero-fill 1.88); the fully-reserved, completely-unseen-site holdout (`testum` + the two sealed 4TU sites) also beat both baselines (trained 2.59 vs random-init not computed there / zero-fill 5.90 / mean-fill 8.56) — real evidence of transfer beyond memorising the training sites, not merely fitting them. A real bug was caught and fixed mid-run: per-window MAD normalization blew up to values in the millions on a handful of near-constant leading-sample windows in TestUM's crosshole files (a real, previously-documented instrument artifact), which a defensive clip (`NORMALIZED_CLIP_MAGNITUDE`) resolved — the corpus was retrained from scratch after the fix for full train/eval consistency. **Exploratory BAM probe** (Section 17C, one site, 4 real targets, explicitly NOT Detector V1 validation): the pretrained encoder reached a clearly lower probe-head BCE loss than a random-init control (0.016 vs 0.095), but neither model showed real positive-vs-negative rank discrimination at the actual target cells (both below the 0.5-chance line on a rank metric) — reported honestly as inconclusive on localisation specifically, not spun into a detection claim. Held back from A: only one seed, one V1 run, and validation loss ticked up between epoch 2 and 3 (mild overfitting signature) — real, promising transfer evidence, not an exhaustive one. **Robustness ablation, same corpus/split/architecture throughout, only the named variable changed**: (1) 3-seed replication (trace-block, 3 epochs) found real seed sensitivity in validation loss (mean 0.930, std 0.124 — 13% CV) but a notably more stable reserved unseen-site loss (mean 2.297, std 0.092 — 4% CV); every seed still clearly beat both baselines. (2) An 8-epoch duration run with validation-only best-checkpoint selection (never the reserved set) found a non-monotonic validation curve peaking at epoch 5 (val 0.845, reserved 2.103) — better than any fixed-3-epoch run, showing the original "epoch-3 degradation" was premature stopping, not fundamental overfitting. (3) Patch (trace×time) masking vs. trace-block, one seed: patch fit validation worse (1.215 vs 1.070) but generalised to the reserved set better (1.315 vs 2.219) — a real, unresolved trade-off, not yet replicated. (4) No feature collapse (`training/ssl_ablation.py` representation check: 0% dead dimensions) and no strong trivial vendor/site clustering in pooled bottleneck features. **Canonical checkpoint** selected on validation/reserved evidence alone (never the BAM probe, rerun once for reference: loss 0.016, consistent with the original run): the 8-epoch best-validation checkpoint — architecture and masking unchanged from V1, since no tested configuration was clearly and repeatably superior enough to change the original design. **Downgraded from a flat "B — PROMISING" to "B — PROMISING BUT VARIABLE"**: the direction of the SSL benefit (beats baselines, beats random-init, generalises to sealed unseen sites) never failed across 5 independent runs, but exact numbers are seed- and duration-sensitive enough that calling it robust would overclaim. Detector V1 remains unaffected and fully blocked — still 4 real labels, 1 site, not resumed |
| Author / evidence requests | 🟡 Open, narrower than before | The 4TU author replied once already and resolved the vertical-datum question (WGS84 ellipsoidal, confirmed by measurement against AHN) — see "External evidence: the 4TU author replied" below. What is still open is two specific questions, not the whole topic: the time-zero/air-gap magnitude, and whether a propagation velocity was ever determined. A follow-up letter draft exists (`docs/4tu-author-letter-draft.md`) |
| Authentication and ownership | ✅ Complete | `docs/authentication.md`; sessions, PBKDF2, dataset ownership, login limiting, password reset with Resend delivery |
| Dataset reports | ✅ Complete | `docs/dataset-report.md`; `GET /api/datasets/{id}/report`, eight capability assessments per dataset |
| Depth-axis origin → ground | ✅ Complete | `docs/depth-origin.md`; a declared offset now participates in the vertical assessment instead of being recorded and ignored |
| Time-zero correction | ✅ Complete, wired into a live endpoint | `schemas/time_zero.py`, `preprocessing/time_zero.py` (33 tests); `POST /api/datasets/{id}/apply_time_zero` (9cae692, 53 tests) is the first live caller — until this endpoint existed the framework was implemented and tested but never invoked by any real path. Three explicit methods, never guessed: Method A (`metadata_instrument_time_zero`, SEG-Y `DelayRecordingTime` only — explicitly refuses GSSI `rhf_position`/MALA `SIGNAL POSITION`), Method B (`operator_declared_time_zero`, wired into `DeclarationKind.TIME_ZERO`), Method C (`direct_wave_consensus_time_zero`, the one algorithmic method — robust median+MAD cross-trace consensus on the direct/coupling-wave onset; DTW considered and rejected, see the module docstring). Resolved PER FRAME, never per-dataset — three real 4TU files gave genuinely different t0 (0.776/0.873/1.764 ns), so a single dataset-wide constant would misrepresent every one. `original_time_ns` is never overwritten; `corrected_time_ns` is additive; a negative corrected time is excluded, not clamped. Depth is recomputed from the corrected axis using each frame's own already-recorded velocity (never a new estimate) and is explicitly cleared, not left stale, where no velocity is known. Depth's own `ProvenanceClass` is never upgraded merely because a correction exists. Verified against real, live production data (not a fixture): ingesting 4TU `01.1/Path8.sgy` through the actual upload path and calling the endpoint produced a genuine MEASURED result via Method A (SEG-Y `DelayRecordingTime`, 2.641 ns) — stronger evidence than Method C's DERIVED results, and a finding this session would not have made without actually wiring the endpoint into a live path rather than stopping at unit tests |
| Surface reference / vertical anchor | ✅ Complete | `docs/surface-reference.md`; a raster band can be declared elevation, so `surface_reference` can reach `available` for the first time. Extended 2026-08-22: the same inference now covers DEM, not only LIDAR |
| Device abstraction | ✅ Complete, capability extended | `docs/devices.md`; device + session records converging on the Stage 9 acquisition boundary. No hardware integration. Extended 2026-08-22: registration offers the full sensor-type enum with no default (was hardcoded `gpr`), and a device can declare it also produces additional modalities beyond its primary type, both readable on the saved card |
| FileDrop acquisition | ✅ Complete | `docs/filedrop.md`; acquisition boundary, checksum at receipt, identification before ingestion, review hold |
| Spatial reference workflow | ✅ Complete | `docs/spatial-reference.md`; seven-dimension assessment, append-only declaration log, seven declaration kinds. **Multi-point affine registration — implemented, pending real-data exercise** (`schemas/spatial.py::AffineTie`, `ingestion/affine_tie.py`, `tests/test_affine_tie.py`, 24 tests + 8 in `tests/test_spatial_reference.py`, all passing): a genuine 2D counterpart to `GeoTie` for a frame whose native position is `LocalCartesianPosition` (a real (x, y), no along-track axis for GeoTie's 1D interpolation to use) — a gap the schema already anticipated but no converter had a route to close. Fits a full 2D affine map from ≥3 non-collinear surveyed control points, reports RMS/max residual in metres (never a flattering 0.0 for an exact 3-point fit), rejects collinear/duplicate/non-finite/numerically-unstable geometry outright, and is fully reversible. Registration is additive — native coordinates are never touched, only `registered_position` is set — and provenance correctly reports `registered`, not `measured` (a related bug in `assess_horizontal`'s tied-detection, which checked only `geo_tie`, was found and fixed in the same change). Browser-verified end to end on a synthetic, explicitly-labelled test dataset, cleaned up after. **It does not advance the localisation gate below**: the repository currently holds no real dataset with a `LocalCartesianPosition` — no converter produces one — so this closes an architectural gap, not a data one. Recommended next step: a real ingestion path (e.g. total-station or laser-scanner data) that genuinely produces `LocalCartesianPosition` with surveyed control points, so the existing implementation can be residual-validated against real geometry instead of remaining exercised only by fixtures |
| Dataset lifecycle management | ✅ Complete | `docs/dataset-lifecycle.md`; rename, safe delete, derived status, duplicate detection, rescore |
| Production-ready platform | ⏳ Later | no encryption at rest, no dataset signing |
| Velocity model estimation, topographic correction & migration | 📋 Future / advanced GPR processing — investigation only, not implemented, **not required for current product readiness** | Dependency chain, in order: velocity estimation → topographic correction → migration; a later stage is not investigated until the one before it is defensible. Depth today uses a hardcoded typical-soil velocity, tagged `ASSUMED` everywhere it appears; a future per-dataset estimate (hyperbola fitting, CMP analysis, or another method the dataset actually supports) may earn `DERIVED` provenance, carrying its method, source evidence and confidence — but only where the evidence justifies it, never automatically, and `ASSUMED` must remain the honest answer where it does not. Topographic correction, once reliable elevation exists (DEM, GNSS/RTK, the existing vertical-reference infrastructure), needs its own physical/geometric justification, not visual alignment. Migration (Kirchhoff/F-K) is investigated only once a defensible velocity model exists, is not accepted merely for visually cleaner anomalies, and must retain its own provenance (algorithm, velocity model and its provenance, topographic-correction status). Explicitly out of scope for this milestone: spatial interpolation (kriging, splines, deep-learning super-resolution) and mesh/volumetric/isosurface reconstruction, which fabricate continuity between sparse measurements the sensors never recorded, and a renderer replacement (e.g. a game engine) undertaken to make sparse evidence look physically complete — all of which conflict with the composition-honesty principle above. **Declared-permittivity velocity — implemented (f1eef90, `ingestion/four_tu_velocity.py`), not required for current product readiness.** 4TU's 125 activities each publish a relative permittivity in `Metadata.csv`; `v = c/√εᵣ` now resolves it into a dataset-specific, opt-in velocity for `4tu_<LocationID>` SEG-Y ingestion, with correct provenance (`DECLARED_BY_SOURCE` εᵣ → `DERIVED` velocity → `DERIVED` depth, `verified=False` throughout) and zero change to `DEFAULT_GPR_VELOCITY_M_PER_NS` or any other dataset. This is source-integration, not empirical validation: the Pk050 audit (`artifacts/bam/pk050_velocity_audit.json`) has since completed and also classified FAILED — neither BAM specimen breaks Pk266's t0/velocity confound, so no held dataset has produced a `verified=True` velocity. Migration remains gated on that, unchanged. **Time-zero is now a known, independently-established quantity for some datasets (see the row above), not necessarily a joint unknown with velocity.** Where Method A or C resolves a real t0 for a line, a velocity fit against that line's known/expected reflector depth no longer needs to solve for both t0 and velocity simultaneously — this does not retroactively fix the BAM/TestUM confound already recorded (those results stand as investigated), but it is a real, evidence-based reason to re-attempt velocity estimation on a dataset where time-zero has actually resolved, rather than assuming the same confound applies everywhere. **That re-attempt was made** (`scripts/bam_independent_t0_velocity_audit.py`, `artifacts/bam/bam_independent_t0_velocity_audit.json`) — a genuinely new angle, not a retry: instead of adding more known-depth evidence (Pk050's approach, which still classified FAILED), t0 is REMOVED as a free parameter, fixed from `preprocessing.time_zero.direct_wave_consensus_time_zero` applied to Pk266's own real traces, leaving a 1-parameter fit per target instead of the original 2-parameter joint fit. Run across all four real Pk266 scan variants (1.5/2.6 GHz x Rot00/Rot90): per-target velocity self-consistency is strong in every scan (2-5% spread, down from "not identifiable"), and the independently-derived t0 is itself remarkably stable within a frequency regardless of rotation (2.6 GHz: 0.2935 ns in both rotations; 1.5 GHz: 0.68-0.73 ns in both) — real physical evidence this reflects antenna hardware, not noise. One scan (1.5 GHz Rot00) passes every check including the independent hyperbola-curvature cross-check and reaches this audit's own `VALIDATED VELOCITY` classification — the strongest result any BAM or TestUM velocity audit has produced in this project's history. The other three scans classify `ESTIMATED BUT NOT VALIDATED`: self-consistency and depth accuracy pass, but the cross-check against Method B's independent curvature-based velocity disagrees materially (18.6% / 35.8% / 132.6%) — reported honestly as a failed check, not rounded up. **This is a research-audit classification, not a live product state**: no `SubterraRecord`, converter, or provenance schema is touched, and no live dataset's velocity carries `verified=True` as a result. The original Pk266 hyperbola audit's own `INCONCLUSIVE` result (default 2.6 GHz Rot00 scan, t0 jointly fit) is unchanged and stands alongside this one, not replaced by it. **Topographic correction was investigated next, from real evidence, on the one held dataset that has both a per-trace acquisition elevation and a matched real DEM: 4TU** (`scripts/four_tu_topographic_correction_audit.py`, `artifacts/4tu/topographic_correction_audit.json`). The physical question asked precisely: does 4TU's antenna height above ground actually VARY along a real line (a genuine air-gap that needs a per-trace correction), or did the acquisition track terrain closely enough that a single constant time-zero correction is already sufficient? Real per-trace antenna elevation (already available via `coordinate_encoding="ieee_nmea"`) was compared against real AHN DTM ground elevation (0.5 m resolution, already fetched and matched per-site by `scripts/acquire_ahn_windows.py`) via the SAME reprojection (`rasterio.warp.transform`) and bilinear sampling (`preprocessing.dem_alignment.sample_dem_bilinear`) this codebase already trusts elsewhere — reused, not reimplemented. Neither elevation source declares a vertical datum, so only the RELATIVE deviation from each line's own median height-above-ground is used; a constant datum offset between the two sources cancels out of a differential correction by construction, so not knowing that offset does not block this conclusion. Across all 4 real lines checked (3 different sites): height-above-ground genuinely varies by 5-16 cm along a single line (independent of the antenna's absolute-elevation-alone range, which correlates only partially), converting to a two-way air-time correction of 0.19-0.60 ns — in every one of the 4 lines, this EXCEEDS that line's own sample interval (~0.097 ns), meaning the correction is resolvable against the acquisition's own temporal resolution, not fabricated precision. **This is real, evidence-based justification for a per-trace topographic/air-gap correction capability — currently a research-audit finding only, not implemented as a correction and not wired into any live dataset.** It does not unblock 4TU depth on its own: velocity remains unvalidated there and the author's own confirmed absence of an air-gap correction is a separate, larger open question this finding narrows but does not resolve |

## The product sequence

The work-area table above records what exists. The **development sequence** is
separate and is reproduced here because it previously lived nowhere in the
repository:

1–5 product shell, upload/import, ownership/auth, password recovery, email
delivery — **complete**. 6 dataset reports — **complete**. 7 dataset
management — **complete**. 8 spatial reference workflow — **complete**. 9 FileDrop acquisition —
**complete**. 10 device abstraction — **complete**. 11 surface reference — **complete**. 12 depth-axis origin — **complete**.
13 candidate intelligence — **complete**: the candidate layer is real,
provenanced, versioned, staleable and inspectable. The detector it exposes is
still at chance, which is a measured result rather than a gap in this stage.
15 radargram inspection — **complete**: the measured B-scan with candidate
overlays mapped exactly to their supporting cells. 16 record-loading performance
— **complete**: the candidate path was parsing its own copy of the corpus, so a
radargram page materialised 384 MB twice; concurrent grid+candidates went from
28.2 s to 4.7 s and candidate retrieval from 22.9 s to 0.27 s. See
`docs/record-loading.md`.
17 amplitude inspection toggle — **complete**: the radargram can now show either
the local-anomaly z-score or the pre-anomaly signal it was computed from, as two
projections of one grid. Candidate footprints, axes and the reliability mask are
identical between them; no unit is claimed for the pre-anomaly values because
none is established. See `docs/radargram-display-modes.md`.
14 ground-truth benchmarks — **complete**, with a negative headline: after the
duplicate audit the 4TU corpus holds 107 independent positives and **6**
independent negatives, which could only distinguish a detector of AUC 0.742 or
better from chance. Six further attested-empty surveys would make a clearly
useful detector (AUC 0.70) recognisable. See `docs/ground-truth-benchmarks.md`.

The old list's "11 acquisition sessions" is obsolete: stage 10 implemented
them. Stage 11 was chosen by dependency instead — the surface anchor was the
only blocker toward reconstruction that needed no external evidence. What
remains: a hardware adapter (blocked: no instrument or protocol identified),
15 validated object detection,
16 multi-modal fusion, 17 3D reconstruction, 18 interactive underground model,
19 real-time scanning, 20 non-expert interpretation.

Stages 17–18 depend on 8 far more than on 13–16, and stage 8 is blocked by
evidence rather than effort. `docs/dataset-report.md` carries the measured
dependency report.

## The product was running an unbenchmarked detector (stage 18)

`run_pipeline(mode="gpr_local_anomaly")` applied the ring statistic WITHOUT the
trace filters that every benchmarked path applies. On a real 4TU line the two
compositions disagree about 95.7% of cells, and the count of cells that become
candidates differs 4.2x (39 unfiltered against 164 filtered).

The two anomaly IMPLEMENTATIONS were always equivalent — `validate_arraywise`
measures them bitwise identical and was never wrong. What diverged was the
composition the ingest pipeline used. `--verify-arraywise`, referenced in three
docstrings as though it were a CLI flag, has never existed.

The owner is the ingest COMPOSITION, not either anomaly function: every ingest
route applies exactly ONE `run_pipeline` mode, so the validated two-step chain
the regression baseline has pinned since the interpretation baseline was
unreachable through the API. A new `gpr_full` mode composes it; the single-step
modes are unchanged, and BAM is bit-identical. Stage 19 then made `gpr_full` the
GPR ingest DEFAULT — a GPR dataset ingested with no mode named now gets the
benchmark-aligned chain, the resolved mode is recorded on the dataset, explicit
choices still win, no other modality moves and no historical dataset is
reprocessed. Verified on a real 4TU line: the defaulted ingest is bit-identical
to the BAM array path and candidate generation succeeds where UI-ingested GPR
previously reported BLOCKED. See `docs/anomaly-path-equivalence.md`.

## External evidence: the 4TU author replied

Dr. ter Huurne, author of the 4TU dataset, answered a direct enquiry. It is the
first evidence request in this project to produce an answer, and it resolves the
vertical-datum question that has been open since stage 8: **the GNSS elevations
in the exported SEG-Y are ellipsoidal WGS84, not NAP.**

It does not unblock depth. The author is explicit that no time-zero correction
and no air-gap removal were applied, so the ground surface does not necessarily
correspond to depth zero and an air path remains in the data — and gives no
magnitude for it. Physical depth and absolute elevation stay BLOCKED.

The author named neither of the two per-trace elevation fields, which differ by
42.2–45.2 m — attaching the datum to the wrong one is a ~44 m error. **That
question has now been answered by measurement rather than by asking.** Both
fields were compared against AHN, the Dutch national terrain model (PDOK, NAP
orthometric), across 366,019 traces in 107 activities and 12 sites: bytes 41–44
track AHN to −0.83 m, bytes 45–48 sit +43.38 m above it, and that difference
correlates with latitude at −0.999 (planar R² 0.998, residual sd 0.034 m) —
geoid behaviour, not a constant instrument offset, matching the published NL
separation range of 41 m (Groningen) to 47 m (Limburg) in magnitude and
gradient. **Bytes 45–48 hold the ellipsoidal GNSS height; bytes 41–44 an
orthometric NAP-like height.** No platform state was changed. See
`docs/4tu-elevation-field-identification.md` and `docs/4tu-author-evidence.md`.

Depth remains blocked regardless: the author is explicit that no time-zero
correction and no air-gap removal were applied, so a surface elevation does not
locate depth zero. The two remaining external questions are the time-zero/air-gap
magnitude and whether a propagation velocity was ever determined.

## Where detection actually stands

The baseline detector is the scientific reference and remains unchanged in the
default path. It is at approximately chance on both benchmarks:

- BAM: recall 0.065 (1.5 GHz) and 0.093 (2.6 GHz); precision 0.135 and 0.147
  against a 0.1297 chance rate — 1.04× and 1.13× chance.
- 4TU: AUC 0.4452, Spearman ρ −0.0619.

Stage 13 reproduced all three of those numbers bit-identically from the current
repository rather than trusting this file, and **corrected one claim**: "at or
below chance" overstated the 4TU result. The separation rests on seven
negatives, and its bootstrap 95% interval is [0.2219, 0.6607] — it spans chance
in both directions, so that benchmark cannot distinguish this method from
chance *either way*. See `docs/candidate-intelligence.md`.

Stage 13 also audited the 4TU corpus for duplicate evaluation units and found
them: 759 radargrams carry 721 unique checksums, six activities are duplicated
in full, and activity 09.7 — one of the seven negatives — shares a
byte-identical radargram with 09.6, a positive. Counting each measurement once
gives 121 activities and AUC 0.4511. The leakage is real and recorded in
`artifacts/4tu/leakage.json`; it is **not** the explanation.

Two candidates have now been tried and both were rejected on evidence.

The **trace-span filter** (Stage 13) required a candidate to span at least K
trace columns, on the physical argument that an object occupying space produces
a laterally continuous response. K was chosen on the Rot90 rotation and reported
on Rot00. Calibration selected K=1 — the baseline. Detections fall 333 → 68 → 0
across K = 1, 2, 3, so **essentially no candidate this detector produces spans
three traces**. That is the most specific account yet of why it sits at chance:
the estimator is responding to near-point excursions, not to laterally extended
structure. `artifacts/experiment/trace_span.json`.

The **multi-scale ring estimator** was designed
against the *measured* width-saturation mechanism, demonstrated that it escapes
that collapse synthetically, and was **rejected**: its BAM gain is concentrated
entirely in duct-4 while ducts 1–3 fall to zero, and 4TU AUC did not improve
(0.4452 → 0.4216, bootstrap ΔAUC interval spanning zero). An earlier claim of
improved SNR and noise behaviour was **withdrawn** — it had been measured on
pure Gaussian noise, and on real radargrams the candidate fires 3.5×–17× more.

Two practices from that experiment are now load-bearing for any future one:

1. **Bit-identical baseline reproduction.** The baseline arm must reproduce the
   frozen artifacts to full precision through the new code path. This is what
   caught a traversal bug that had silently scored 123 of 125 activities and
   produced a plausible wrong answer (AUC 0.4429 vs a true 0.4452).
2. **Pre-registered calibration**, never tuned against benchmark truth.

## What blocks localisation

Not effort — evidence. 4TU publishes no trench coordinates, so no candidate can
be matched to a utility. BAM's absolute origin is unverified. No dataset held
has an established vertical relationship: the GPR time axis starts at
instrument time-zero, not the ground surface, and no source declares a vertical
datum. The backend states this itself when asked to resolve a 3D scene, which
is why `scene_3d` is unavailable for every dataset and why the workspace draws
no subsurface geometry.

Detection scoring does not depend on any of this and is unaffected. A blocked
localisation gate does not make the detection numbers invalid, and the
detection numbers do not make the gate any less blocked.
