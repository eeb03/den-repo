fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
psycopg2-binary==2.9.9
geoalchemy2==0.15.2
pandas==2.2.3
numpy==2.1.1
scipy==1.14.1
requests==2.32.3
python-multipart==0.0.9

# Optional format converters (converters degrade gracefully if missing).
# Version floors, not exact pins: exact pins can force pip into a source
# build on platforms (e.g. Docker Desktop arm64 on Apple Silicon) where
# that pinned version has no prebuilt wheel.
segyio>=1.9.12
laspy>=2.5.4
rasterio>=1.3.10
obspy>=1.4.1

# Visualization (Phase 2)
plotly==5.24.1

# Learned Detector V1 (training/segmentation_model.py): CPU-only build,
# gracefully degrades (MissingDependencyError) if absent -- same pattern as
# the format converters above. --extra-index-url is scoped to this one
# package by pip's own resolution (the default PyPI index is tried too;
# torch resolves here to the CPU wheel rather than pulling the much larger
# CUDA-bundled default), never a GPU/CUDA requirement.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.9.0

# Real GPR Annotation Corpus V1 (training/segmentation.py's visual QA):
# was present transitively (a torch dependency) but not made explicit --
# pinned here so it stays available regardless of how torch's own
# dependency graph changes. Same optional/graceful-degradation pattern.
matplotlib>=3.9.0

# Testing
pytest==8.3.3
httpx==0.27.2
